@inherits LayoutComponentBase

@* The layout for statically rendered pages ([ExcludeFromInteractiveRouting] — see
   docs/design/weekly-charts-overhaul.md §3.4). MainLayout is circuit-shaped: the recap dialog
   would static-render stuck open, and its LocationChanged wiring, dock service and JS-interop
   lifecycle assume a circuit exists. A static page opts out of all of it. The shell chrome
   already wraps every page from App.razor; a page that wants dock markup emits it itself.
   Static regions are --mix-*-only and never lean on a Mud popover (static-shell.md D5). *@
<MudContainer>
    @Body
</MudContainer>
