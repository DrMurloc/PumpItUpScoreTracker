@page "/Community/Leaderboard"
@rendermode RenderModes.Interactive
@using MediatR
@using ScoreTracker.Communities.Contracts.Queries
@using ScoreTracker.Domain.Models
@using ScoreTracker.Domain.Records
@using ScoreTracker.Domain.SecondaryPorts
@using ScoreTracker.PlayerProgress.Contracts.Queries
@using ScoreTracker.SharedKernel.Enums
@using ScoreTracker.SharedKernel.Models
@using ScoreTracker.SharedKernel.ValueTypes
@using ScoreTracker.Web.Components
@using ScoreTracker.Web.Services.Theming
@using System.Web
@using Chart = ScoreTracker.SharedKernel.Models.Chart
@using ChartType = ScoreTracker.SharedKernel.Enums.ChartType
<PageTitle>@L["Communities"]</PageTitle>

@if (_community != null)
{
    <div class="d-flex align-end justify-space-between flex-wrap mb-2" style="gap:12px">
        <div>
            <MudText Typo="Typo.h4">@((string)_community.Name)</MudText>
            <MudText Typo="Typo.body2" Class="mud-text-secondary">@L["{0} members", _leaderboard.Count()]</MudText>
        </div>
        @if (_community.IsRegional)
        {
            <MudSwitch T="bool" Label="@L["Show Regional Guests"]" Value="_showGuests" ValueChanged="SetShowGuests" Color="Color.Primary"></MudSwitch>
        }
    </div>

    <MudTabs Elevation="0" ApplyEffectsToContainer="true" Class="mb-4">
        <MudTabPanel Text="@L["Rankings"]">
            <MudStack Row="true" Spacing="2" AlignItems="AlignItems.Center" Class="mb-2 mt-3 flex-wrap">
                <MudSelect T="LeaderboardType" Value="_currentType" ValueChanged="SetType" Label="@L["Board"]"
                           Dense="true" Style="max-width:200px">
                    <MudSelectItem T="LeaderboardType" Value="LeaderboardType.Combined">PUMBILITY</MudSelectItem>
                    <MudSelectItem T="LeaderboardType" Value="LeaderboardType.Total">@L["Total Rating"]</MudSelectItem>
                    <MudSelectItem T="LeaderboardType" Value="LeaderboardType.CoOp">@L["CoOp"]</MudSelectItem>
                </MudSelect>
                <MudSpacer/>
                <MudText Typo="Typo.caption" Class="mud-text-secondary">
                    @L["Tap a rating to expand the player's top charts."]
                </MudText>
            </MudStack>

            <MudTable T="LeaderboardEntry" Items="_entries" Breakpoint="Breakpoint.None" Dense="true"
                      RowsPerPage="50" Class="olb-sticky-second olb-pager-tight">
                <HeaderContent>
                    <MudTh>@L["Rank"]</MudTh>
                    <MudTh>@L["Player"]</MudTh>
                    <MudTh Style="text-align:right">@(_currentType == LeaderboardType.Combined ? "PUMBILITY" : _currentType == LeaderboardType.Total ? L["Total Rating"] : L["CoOp"])</MudTh>
                    @if (_currentType == LeaderboardType.Combined)
                    {
                        <MudTh Style="text-align:right" Class="olb-hide-mobile">@L["Singles"]</MudTh>
                        <MudTh Style="text-align:right" Class="olb-hide-mobile">@L["Doubles"]</MudTh>
                        <MudTh Style="text-align:right">@L["Comp Lv"]</MudTh>
                    }
                    @if (_currentType == LeaderboardType.Total)
                    {
                        <MudTh Style="text-align:right">@L["Highest Level"]</MudTh>
                        <MudTh Style="text-align:right">@L["Clear Count"]</MudTh>
                    }
                </HeaderContent>
                <RowTemplate>
                    <MudTd>
                        <RankDelta Rank="context.Place"/>
                    </MudTd>
                    <MudTd>
                        <div style="display:flex;align-items:center;gap:8px">
                            <MudImage Src="@context.ProfileImage.ToString()" Height="28" ObjectFit="ObjectFit.ScaleDown"></MudImage>
                            <UserLabel User="_users[context.UserId]"></UserLabel>
                            @if (context.IsPublic)
                            {
                                <MudTooltip Text="@L["Phoenix Recap"]">
                                    <MudIconButton Size="Size.Small" Icon="@Icons.Material.Filled.LocalFireDepartment"
                                                   Href=@($"/Player/{context.UserId}/PhoenixRecap")></MudIconButton>
                                </MudTooltip>
                            }
                        </div>
                    </MudTd>
                    <MudTd Style="text-align:right">
                        @if (_currentType == LeaderboardType.Combined)
                        {
                            <MudLink Style=@($"color:{context.Color};cursor:pointer") Underline="Underline.None"
                                     OnClick="() => ToggleExpand(context.UserId, null, false)"><b>@context.Rating</b></MudLink>
                        }
                        else
                        {
                            <b style="color:@context.Color">@context.Rating</b>
                        }
                    </MudTd>
                    @if (_currentType == LeaderboardType.Combined)
                    {
                        <MudTd Style="text-align:right" Class="olb-hide-mobile">
                            <MudLink Style=@($"color:{_singlesScale.GetColor(context.SinglesRating)};cursor:pointer") Underline="Underline.None"
                                     OnClick="() => ToggleExpand(context.UserId, ChartType.Single, false)">@context.SinglesRating</MudLink>
                        </MudTd>
                        <MudTd Style="text-align:right" Class="olb-hide-mobile">
                            <MudLink Style=@($"color:{_doublesScale.GetColor(context.DoublesRating)};cursor:pointer") Underline="Underline.None"
                                     OnClick="() => ToggleExpand(context.UserId, ChartType.Double, false)">@context.DoublesRating</MudLink>
                        </MudTd>
                        <MudTd Style="text-align:right">
                            <MudLink Style="cursor:pointer" Underline="Underline.None"
                                     OnClick="() => ToggleExpand(context.UserId, null, true)">@context.CompetitiveLevel.ToString("0.00")</MudLink>
                        </MudTd>
                    }
                    @if (_currentType == LeaderboardType.Total)
                    {
                        <MudTd Style="text-align:right">@context.HighestLevel</MudTd>
                        <MudTd Style="text-align:right">@context.ClearCount</MudTd>
                    }
                </RowTemplate>
                <ChildRowContent>
                    @if (_expandedUserId == context.UserId)
                    {
                        <td colspan="@Colspan">
                            @if (_scores == null)
                            {
                                <MudSkeleton Height="120px" SkeletonType="SkeletonType.Rectangle"/>
                            }
                            else
                            {
                                <div class="tier-card-grid pa-2">
                                    @foreach (var entry in _scores)
                                    {
                                        <TierListChartCard Chart="entry.Chart"
                                                           Score="new RecordedPhoenixScore(entry.Chart.Id, entry.Score, entry.Plate, entry.IsBroken, default)"
                                                           TierLabel="@(_expandedCompetitive ? entry.CompetitiveLevel.ToString("0.00") : entry.Rating.ToString("N0"))"
                                                           ShowToDo="false"/>
                                    }
                                </div>
                            }
                        </td>
                    }
                </ChildRowContent>
                <PagerContent>
                    <MudTablePager HideRowsPerPage="true"/>
                </PagerContent>
            </MudTable>
        </MudTabPanel>

        <MudTabPanel Text="@L["By Chart"]">
            <MudGrid Class="mt-3">
                <MudItem xs="12" sm="6" md="4">
                    <ChartSelector ChartIdSelected="SelectChart" Disabled="_selectingChart"></ChartSelector>
                </MudItem>
                <MudItem xs="12">
                    @if (!_communityChartScores.Any())
                    {
                        if (_hasSelectedChart)
                        {
                            <MudText>@L["No scores for the chart recorded in this community."]</MudText>
                        }
                        else
                        {
                            <MudText>@L["Select a chart to see this community's leaderboard!"]</MudText>
                        }
                    }
                </MudItem>
                @if (_communityChartScores.Any())
                {
                    <MudTable T="(int, UserPhoenixScore)" Items="_communityChartScores.OrderByDescending(s => s.Score).Select((s, i) => (i + 1, s))" Breakpoint="Breakpoint.None" Dense="true">
                        <HeaderContent>
                            <MudTh>@L["Rank"]</MudTh>
                            <MudTh>@L["Player"]</MudTh>
                            <MudTh>@L["Score"]</MudTh>
                        </HeaderContent>
                        <RowTemplate>
                            <MudTd>
                                <RankDelta Rank="context.Item1"/>
                            </MudTd>
                            <MudTd>
                                <div style="display:flex;align-items:center;gap:8px">
                                    @if (_nameAvatars.TryGetValue(context.Item2.UserId, out var avatarUrl))
                                    {
                                        <MudImage Src="@avatarUrl" Height="28" ObjectFit="ObjectFit.ScaleDown"></MudImage>
                                    }
                                    @context.Item2.UserName
                                </div>
                            </MudTd>
                            <MudTd><ScoreBreakdown Score="context.Item2.Score" Plate="context.Item2.Plate" IsBroken="context.Item2.IsBroken" ShowScore="true"></ScoreBreakdown></MudTd>
                        </RowTemplate>
                    </MudTable>
                }
            </MudGrid>
        </MudTabPanel>

        <MudTabPanel Text="@L["Members"]">
            <div class="mt-3">
                <CommunityMembers/>
            </div>
        </MudTabPanel>
    </MudTabs>
}

@inject IMediator Mediator;
@inject ICurrentUserAccessor CurrentUser;
@inject NavigationManager NavManager;
@inject IUserRepository Users;
@inject ScoreTracker.Web.Services.Contracts.IUiSettingsAccessor UiSettings;
@code {
    // Community stats are Phoenix-lineage — XX viewers keep seeing Phoenix, as before.
    private MixEnum _currentMix = MixEnum.Phoenix;

    private Community? _community;
    private IEnumerable<CommunityLeaderboardRecord> _leaderboard = Array.Empty<CommunityLeaderboardRecord>();
    private IEnumerable<LeaderboardEntry> _entries = Array.Empty<LeaderboardEntry>();
    private IDictionary<Guid, Chart> _charts = new Dictionary<Guid, Chart>();
    private IDictionary<Guid, User> _users = new Dictionary<Guid, User>();
    private IDictionary<Guid, string> _nameAvatars = new Dictionary<Guid, string>();
    private ISet<Name> _myCommunities = new HashSet<Name>();
    private bool _showGuests = false;

    // One percentile scale per rating column: a player's color reflects where they sit in
    // THIS community's distribution, on the sitewide rarity ramp.
    private ThemeScales.PopulationScale _totalScale = ThemeScales.ScaleFrom(Array.Empty<int>());
    private ThemeScales.PopulationScale _skillScale = ThemeScales.ScaleFrom(Array.Empty<int>());
    private ThemeScales.PopulationScale _singlesScale = ThemeScales.ScaleFrom(Array.Empty<int>());
    private ThemeScales.PopulationScale _doublesScale = ThemeScales.ScaleFrom(Array.Empty<int>());
    private ThemeScales.PopulationScale _coOpScale = ThemeScales.ScaleFrom(Array.Empty<int>());

    private LeaderboardType _currentType = LeaderboardType.Combined;
    private int Colspan => _currentType == LeaderboardType.Combined ? 6 : _currentType == LeaderboardType.Total ? 5 : 3;

    private Guid _expandedUserId = Guid.Empty;
    private ChartType? _expandedChartType;
    private bool _expandedCompetitive;
    private ScoreEntry[]? _scores;

    private IEnumerable<UserPhoenixScore> _communityChartScores = Array.Empty<UserPhoenixScore>();
    private bool _hasSelectedChart = false;
    private bool _selectingChart = false;
    private Chart? _selectedChart;

    [Parameter]
    [SupplyParameterFromQuery] public string CommunityName { get; set; } = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        var communityName = HttpUtility.UrlDecode(CommunityName);
        var selectedMix = await UiSettings.GetSelectedMix();
        _currentMix = selectedMix == MixEnum.XX ? MixEnum.Phoenix : selectedMix;
        var community = await Mediator.Send(new GetCommunityQuery(communityName));
        _community = community;
        _charts = (await Mediator.Send(new GetChartsQuery(_currentMix))).ToDictionary(c => c.Id);
        _myCommunities = (await Mediator.Send(new GetMyCommunitiesQuery())).Select(c => c.CommunityName).ToHashSet();
        if (community.PrivacyType == CommunityPrivacyType.Private)
        {
            if (!CurrentUser.IsLoggedIn || !_myCommunities.Contains(communityName))
            {
                NavManager.NavigateTo("/Communities");
                return;
            }
        }

        _leaderboard = (await Mediator.Send(new GetCommunityLeaderboardQuery(communityName, _currentMix))).ToArray();
        _nameAvatars = _leaderboard.Where(l => l.IsPublic).ToDictionary(l => l.UserId, l => l.ProfileImage.ToString());
        _users = (await Users.GetUsers(_leaderboard.Select(l => l.UserId).Distinct())).ToDictionary(u => u.Id);
        _totalScale = ThemeScales.ScaleFrom(_leaderboard.Select(l => (int)l.TotalRating));
        _skillScale = ThemeScales.ScaleFrom(_leaderboard.Select(l => (int)l.SkillRating));
        _singlesScale = ThemeScales.ScaleFrom(_leaderboard.Select(l => (int)l.SinglesRating));
        _doublesScale = ThemeScales.ScaleFrom(_leaderboard.Select(l => (int)l.DoublesRating));
        _coOpScale = ThemeScales.ScaleFrom(_leaderboard.Select(l => (int)l.CoOpRating));
        SetType(LeaderboardType.Combined);
    }

    private void SetType(LeaderboardType type)
    {
        if (_community == null) return;
        _expandedUserId = Guid.Empty;
        _scores = null;
        var entries = _leaderboard.Where(l => !_community.IsRegional || _showGuests || _users[l.UserId].Country == CommunityName);
        _entries = type switch
        {
            LeaderboardType.Total => entries.OrderByDescending(l => l.TotalRating).Select((l, i) =>
                Entry(l, i, l.TotalRating, _totalScale.GetColor((int)l.TotalRating))),
            LeaderboardType.CoOp => entries.OrderByDescending(l => l.CoOpRating).Select((l, i) =>
                Entry(l, i, l.CoOpRating, _coOpScale.GetColor((int)l.CoOpRating))),
            _ => entries.OrderByDescending(l => l.SkillRating).Select((l, i) =>
                Entry(l, i, l.SkillRating, _skillScale.GetColor((int)l.SkillRating)))
        };
        _currentType = type;
    }

    private LeaderboardEntry Entry(CommunityLeaderboardRecord record, int index, int rating, string color) =>
        new(index + 1, record.ProfileImage, record.PlayerName, record.IsPublic, record.UserId, rating,
            record.SinglesRating, record.DoublesRating, record.CompetitiveLevel, color, record.HighestLevel,
            record.ClearCount);

    private void SetShowGuests(bool newValue)
    {
        _showGuests = newValue;
        SetType(_currentType);
        if (_selectedChart != null)
        {
            _ = SelectChart(_selectedChart);
        }
    }

    private async Task ToggleExpand(Guid userId, ChartType? chartType, bool competitive)
    {
        if (_expandedUserId == userId && _expandedChartType == chartType && _expandedCompetitive == competitive)
        {
            _expandedUserId = Guid.Empty;
            _scores = null;
            return;
        }

        _expandedUserId = userId;
        _expandedChartType = chartType;
        _expandedCompetitive = competitive;
        _scores = null;
        var scoringConfig = ScoringConfiguration.PumbilityScoring(_currentMix, true);
        var top50 = competitive
            ? await Mediator.Send(new GetTop50CompetitiveQuery(userId, chartType, Mix: _currentMix))
            : await Mediator.Send(new GetTop50ForPlayerQuery(userId, chartType, Mix: _currentMix));
        _scores = top50.Where(s => _charts.ContainsKey(s.ChartId))
            .Select(s => new ScoreEntry(_charts[s.ChartId], s.Score ?? 0, s.Plate, s.IsBroken,
                (int)scoringConfig.GetScore(_charts[s.ChartId], s.Score ?? 0, s.Plate ?? PhoenixPlate.ExtremeGame, false),
                ScoringConfiguration.CalculateFungScore(_charts[s.ChartId].Level, s.Score ?? 0, _charts[s.ChartId].Type)))
            .ToArray();
    }

    private async Task SelectChart(Chart chart)
    {
        _selectingChart = true;
        _hasSelectedChart = true;
        _selectedChart = chart;
        _communityChartScores = await Mediator.Send(new GetPhoenixRecordsForCommunityQuery(CommunityName, chart.Id, _currentMix));
        if (_community is { IsRegional: true } && !_showGuests)
        {
            _communityChartScores = _communityChartScores
                .Where(c => _users.TryGetValue(c.UserId, out var user) && user.Country == CommunityName).ToArray();
        }
        _selectingChart = false;
        StateHasChanged();
    }

    private sealed record ScoreEntry(Chart Chart, PhoenixScore Score, PhoenixPlate? Plate, bool IsBroken, int Rating, double CompetitiveLevel);

    private sealed record LeaderboardEntry(int Place, Uri ProfileImage, Name PlayerName, bool IsPublic, Guid UserId,
        int Rating, int SinglesRating, int DoublesRating, double CompetitiveLevel, string Color, int HighestLevel,
        int ClearCount);

    private enum LeaderboardType
    {
        Total,
        Combined,
        CoOp
    }
}
